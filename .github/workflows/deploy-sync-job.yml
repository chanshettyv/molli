name: Deploy sync-job

on:
  push:
    branches: [main]
    paths:
      - 'sync-job/**'
      - 'shared/**'
      - '.github/workflows/deploy-sync-job.yml'
  workflow_dispatch:

env:
  PROJECT_ID: molli-prod
  REGION: us-central1
  JOB: molli-sync-job
  REPO: molli

jobs:
  deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - uses: actions/checkout@v4

      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ secrets.GCP_WIF_PROVIDER }}
          service_account: ${{ secrets.GCP_DEPLOY_SA }}

      - uses: google-github-actions/setup-gcloud@v2

      - name: Configure Docker
        run: gcloud auth configure-docker ${{ env.REGION }}-docker.pkg.dev

      - name: Build and push image
        run: |
          IMAGE=${{ env.REGION }}-docker.pkg.dev/${{ env.PROJECT_ID }}/${{ env.REPO }}/${{ env.JOB }}:${{ github.sha }}
          docker build -f sync-job/Dockerfile -t $IMAGE .
          docker push $IMAGE
          echo "IMAGE=$IMAGE" >> $GITHUB_ENV

      - name: Deploy Cloud Run job
        run: |
          gcloud run jobs deploy ${{ env.JOB }} \
            --image=$IMAGE \
            --region=${{ env.REGION }} \
            --service-account=${{ secrets.GCP_RUNTIME_SA }} \
            --max-retries=1
