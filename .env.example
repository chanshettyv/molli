# Non-secret config. Secrets are pulled from GCP Secret Manager at runtime.
# Copy to .env and fill in.

GCP_PROJECT_ID=molli-dev
GCP_REGION=us-central1
ENVIRONMENT=dev
FRESHSERVICE_DOMAIN=preiss
# VECTOR_INDEX_ENDPOINT=
